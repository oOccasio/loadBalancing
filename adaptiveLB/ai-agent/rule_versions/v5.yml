# Generated by AI agent — version 5
# Generated at: 2026-04-03T08:02:00.775751

rules:
  - state: LOW_TRAFFIC
    algorithm: ROUND_ROBIN
    confidence_threshold: 0.7
    description: "With an avg_rps of 85.7 and low latency of 4.7 ms, ROUND_ROBIN is sufficient for evenly distributing the low traffic without complex calculations."

  - state: HIGH_STABLE
    algorithm: LEAST_CONNECTIONS
    confidence_threshold: 0.8
    description: "For avg_rps around 284.8 with stable latency and no errors, LEAST_CONNECTIONS helps maintain stability by ensuring no server is overloaded, as seen in stable-fixed-round_robin results with higher latency."

  - state: SPIKE
    algorithm: WEIGHTED_LEAST_CONNECTIONS
    confidence_threshold: 0.9
    description: "In case of a sudden spike, WEIGHTED_LEAST_CONNECTIONS can adapt quickly by directing traffic to less busy servers, minimizing latency spikes."

  - state: OVERLOADED_NODE
    algorithm: WEIGHTED_LEAST_CONNECTIONS
    confidence_threshold: 0.85
    description: "When specific nodes are overloaded, this algorithm helps by directing traffic away from those nodes, reducing latency and error rates effectively."

  - state: GRADUAL_INCREASE
    algorithm: WEIGHTED_ROUND_ROBIN
    confidence_threshold: 0.75
    description: "As traffic gradually increases, WEIGHTED_ROUND_ROBIN can manage the load by considering server capacities, ensuring balanced distribution and maintaining low latency."

switch_policy:
  cooldown_seconds: 10
  min_confidence: 0.6
  max_switches_per_minute: 3
  sustained_windows: 3
  spike_sustained_windows: 2
