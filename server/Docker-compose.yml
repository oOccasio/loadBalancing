services:
  web-server-1:
    build: .
    ports:
      - "5001:8080"
    environment:
      - SERVER_ID=server-1
      - RESPONSE_DELAY=50

  web-server-2:
    build: .
    ports:
      - "5002:8080"
    environment:
      - SERVER_ID=server-2
      - RESPONSE_DELAY=150

  web-server-3:
    build: .
    ports:
      - "5003:8080"
    environment:
      - SERVER_ID=server-3
      - RESPONSE_DELAY=300

  web-server-4:
    build: .
    ports:
      - "5004:8080"
    environment:
      - SERVER_ID=server-4
      - RESPONSE_DELAY=500